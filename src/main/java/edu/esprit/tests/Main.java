package edu.esprit.tests;
import edu.esprit.entities.Oeuvre;
import edu.esprit.entities.Avis;
import edu.esprit.entities.User;
import edu.esprit.services.ServiceOeuvre;
import edu.esprit.services.ServiceAvis;
import edu.esprit.services.ServicePersonne;
import edu.esprit.utils.DataSource;

public class Main {
    public static void main(String[] args) {
        //DataSource dataSource = DataSource.getInstance();
//
      //  dataSource.getCnx();
//
//
     //   ServicePersonne personne=new ServicePersonne();
     //   ServiceOeuvre oeuvre=new ServiceOeuvre();
     //   ServiceAvis avis=new ServiceAvis();
//
        //Oeuvre e=new Oeuvre("femme0.3","peinture",3200,java.sql.Date.valueOf("2021-12-15"),"ttttttttttt","/images/art/femme");
        //oeuvre.ajouter(e);
        //User p = new User();
        //Avis a = new Avis();

        ///////////////:Oeuvre/::::::::::::::::::::::::::::

////////********ajouterrrrr oeuvre********////////
        //e.setNom("femme0.3");
        //e.setPrix(3000);
        //e.setCategorie("peinture");
        //e.setDateCreation(java.sql.Date.valueOf("2021-12-15"));
        //e.setDescription("ttttttttttt");
        //e.setImage("/images/art/femme");
        //oeuvre.ajouter(e);

        //////////modifier////////
        // Modification de l'oeuvre
       //e.setId(1);
        //e.setNom("femme1.0");
        //e.setPrix(2350);
        //e.setCategorie("peinture moderne");
        //e.setDateCreation(java.sql.Date.valueOf("2019-09-15"));
        //e.setDescription("creation de l artiste domino");
        //e.setImage("/images/art/femme1");
        //oeuvre.modifier(e);
        //oeuvre.supprimer(4);
        //oeuvre.getOneById(1);
        //oeuvre.getAll();

        ///////////////:Avis/::::::::::::::::::::::::::::
        //modifier un avis
        //a.setIdAvis(1);
        //a.setCommentaire("j ai pas aime les couleurs !");
        //a.setDateExperience(java.sql.Date.valueOf("2021-09-16")); // Utilisation de la date actuelle
        //a.setNote(1);
        //Oeuvre oeuvre_add_avis = oeuvre.getOneById(1); // Récupération de l'oeuvre par son ID
        //a.setOeuvre(oeuvre_add_avis); // Association avec l'oeuvre
        //User user_add_avis = personne.getOneById(3); // Récupération de l'utilisateur par son ID
        //a.setClient(user_add_avis); // Association avec l'utilisateur
        //avis.modifier(a); // modification de l'avis à la base de données
        //ajouter un avis
        //a.setCommentaire("excellent travail !");
        //a.setDateExperience(java.sql.Date.valueOf("2022-07-05")); // Utilisation de la date actuelle
        //a.setNote(4);
        //Oeuvre oeuvre_add_avis = oeuvre.getOneById(1); // Récupération de l'oeuvre par son ID
        //a.setOeuvre(oeuvre_add_avis); // Association avec l'oeuvre
        //User user_add_avis = personne.getOneById(4); // Récupération de l'utilisateur par son ID
        //a.setClient(user_add_avis); // Association avec l'utilisateur
        //avis.ajouter(a); // ajout de l'avis à la base de données

        //avis.supprimer(1);

        //ajouter un avis
        //a.setCommentaire("bien!");
        //a.setDateExperience(java.sql.Date.valueOf("2023-02-14")); // Utilisation de la date actuelle
        //a.setNote(3);
        //Oeuvre oeuvre_add_avis = oeuvre.getOneById(6); // Récupération de l'oeuvre par son ID
        //a.setOeuvre(oeuvre_add_avis); // Association avec l'oeuvre
        //User user_add_avis = personne.getOneById(4); // Récupération de l'utilisateur par son ID
        //a.setClient(user_add_avis); // Association avec l'utilisateur
        //avis.ajouter(a); // ajout de l'avis à la base de données
        //System.out.println(avis.getAll());
        //System.out.println(avis.getOneById(2));
        //oeuvre.supprimer(6);

    }
}
